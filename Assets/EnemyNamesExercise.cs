using System.Collections.Generic;
using UnityEngine;

public class EnemyNamesExercise : MonoBehaviour
{
    // Start is called before the first frame update
    List<string> EnemyNames;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
