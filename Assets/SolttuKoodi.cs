using UnityEngine;

public class SolttuKoodi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void Heal(int hp)
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
